
name: Kubernetes CI

on:
  push:
    branches:
      - master

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Install kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Validate Kubernetes YAML
        run: |
          kubectl create --dry-run=client --validate=false -f namespace.yaml
          kubectl create --dry-run=client --validate=false -f configmap.yaml
          kubectl create --dry-run=client --validate=false -f secret.yaml
          kubectl create --dry-run=client --validate=false -f deployment.yaml
          kubectl create --dry-run=client --validate=false -f service.yaml
          kubectl create --dry-run=client --validate=false -f ingress.yaml
